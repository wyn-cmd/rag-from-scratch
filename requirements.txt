# Core pipeline: standard library only, nothing to install.
# Optional stack for real embeddings and real generation:
sentence-transformers>=2.7,<4
transformers>=4.44,<5
torch>=2.3,<3
beautifulsoup4>=4.12,<5
requests>=2.31,<3
